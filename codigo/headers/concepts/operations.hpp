#pragma once

/* Sign function. */
float sgn (float num);

/* Function that return the maximum of 'num' and 0. */
float max0 (float num);

/* Absolute value function. */
float absv (float num);
void absv (float* num);

/* Fast inverse square root. */
float fisqrt (float num);
